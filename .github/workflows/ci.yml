name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    uses: browniebroke/github-actions/.github/workflows/gatsby-site.yml@main
    secrets:
      bundlewatch_token: ${{ secrets.BUNDLEWATCH_GITHUB_TOKEN }}
